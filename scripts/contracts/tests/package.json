{
  "name": "contract-test",
  "version": "0.1.0",
  "description": "CITA contract test using web3",
  "main": "",
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.12.0"
  },
  "scripts": {
    "test": "mocha test --recursive -t 60s",
    "unit_test": "mocha test/unit --recursive -t 60s",
    "unit_group": "mocha test/unit/group.js -t 60s",
    "unit_permission": "mocha test/unit/permission.js  -t 60s",
    "unit_gm": "mocha test/unit/group_management.js -t 60s",
    "unit_pm": "mocha test/unit/permission_management.js -t 60s",
    "unit_rm": "mocha test/unit/role_management.js -t 60s",
    "unit_auth": "mocha test/unit/authorization.js -t 60s",
    "unit_quota": "mocha test/unit/quota.js -t 60s",
    "unit_qm": "mocha test/unit/quota_manager.js -t 60s",
    "unit_node": "mocha test/unit/node_manager.js -t 60s",
    "unit_chain": "mocha test/unit/chain_manager.js -t 60s",
    "unit_admin": "mocha test/unit/admin.js -t 60s",
    "integrate_test": "mocha test/integrate --recursive -t 60s",
    "integrate_call-permission": "mocha test/integrate/call_permission  -t 60s",
    "integrate_call-role": "mocha test/integrate/call_role -t 60s",
    "integrate_call-group": "mocha test/integrate/call_group -t 60s",
    "integrate_deploy_contract": "mocha test/integrate/deploy_contract -t 60s",
    "integrate_send-tx": "mocha test/integrate/send_tx -t 60s",
    "integrate_quota": "mocha test/integrate/quota -t 60s",
    "abi": "mocha test/integrate/abi.js -t 60s",
    "lint": "eslint test",
    "lint-fix": "eslint --fix test",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "test/**/*.js": [
      "eslint --fix",
      "git add"
    ]
  },
  "repository": {},
  "author": "Cryptape Technologies <contact@cryptape.com>",
  "license": "GPL-3.0+",
  "dependencies": {
    "@nervos/web3": "^0.17.32",
    "chai": "^4.1.2",
    "husky": "^0.14.3",
    "lint-staged": "^7.2.0",
    "log4js": "^2.7.0",
    "mocha": "^5.2.0",
    "web3": "^1.0.0-beta.34"
  }
}
